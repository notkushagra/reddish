# reddish

Goal: 
Core idea—store key-value pairs in memory, handle basic commands (GET, SET, DEL), and optionally write to disk.
1. Use RESP protocol
  - Build your own parser.
2. Be redis-cli client compatible.
3. Build a simple web app for demonstration.

